import { FC } from "react";
import LogoutIcon from "components/svgs/logout";
import { Flex } from "../../modules/styles/logout.styles";

const Logout: FC = () => {
  const a = "";
  return (
    <Flex>
      <LogoutIcon />
    </Flex>
  );
};
export default Logout;
